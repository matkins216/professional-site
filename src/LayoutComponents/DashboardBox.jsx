import { Box } from "@mui/material";
import { styled } from "@mui/system";

const DashboardBox = styled(Box)({
    backgroundColor: "#2983f5",
    borderRadius: "1rem",
    boxShadow: "0.15rem 0.2rem 0.15rem 0.1rem rgba(237, 191, 252, 1)",
});

export default DashboardBox;